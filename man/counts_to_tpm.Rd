% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_clean.R
\name{counts_to_tpm}
\alias{counts_to_tpm}
\title{转换计数矩阵为 TPM（每百万转录本）}
\usage{
tpm_matrix <- counts_to_tpm(counts = your_counts_matrix, org = "hsa")
}
\arguments{
\item{counts}{数据框，基因表达的计数矩阵，行为基因，列为样本。每个值应为非负整数，表示每个基因在每个样本中的计数值。}

\item{org}{字符串，指定物种。可选值为 "hsa"（人类，默认）或 "mmus"（小鼠）。根据物种选择，函数会自动设置 \code{idType} 和 \code{source} 参数。}
}
\value{
返回一个数据框，包含转换后的 TPM 矩阵。行对应基因，列对应样本。每个值表示每个基因在每个样本中的 TPM 值。
}
\description{
该函数封装了 \code{count2tpm} 函数，用于将基因表达的计数矩阵转换为 TPM（每百万转录本，Transcripts Per Million）。
用户只需提供表达计数矩阵和物种信息，函数会自动选择适当的参数进行转换。
}
\details{
该函数是对 \code{count2tpm} 函数的封装，旨在简化用户的操作。用户只需提供表达计数矩阵和物种信息，函数会根据物种自动选择适当的 \code{idType} 和 \code{source} 参数。
\itemize{
\item 对于人类（"hsa"），使用 \code{idType = "symbol"} 和 \code{source = "local"}。
\item 对于小鼠（"mmus"），同样使用 \code{idType = "symbol"} 和 \code{source = "local"}。
}

如果物种不同，用户可以根据需要进一步调整 \code{idType} 和 \code{source} 参数。
}
\examples{
\dontrun{
# 示例代码
# 假设您有一个基因表达的计数矩阵 your_counts_matrix

# 将计数矩阵转换为 TPM（默认物种为人类）
tpm_matrix_human <- counts_to_tpm(counts = your_counts_matrix)

# 将计数矩阵转换为 TPM（物种为小鼠）
tpm_matrix_mouse <- counts_to_tpm(counts = your_counts_matrix, org = "mmus")
}

}
\keyword{TPM转换}
\keyword{基因表达计数}
\keyword{物种选择}
