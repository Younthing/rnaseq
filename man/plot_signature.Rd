% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/signature_score.R
\name{plot_signature}
\alias{plot_signature}
\title{单个基因集的表型评分箱线图}
\usage{
plot_signature(
  df_long,
  gene_set,
  x = "group",
  y = "Score",
  fill = "group",
  palette = c("#2E86C1", "#E74C3C", "#27AE60"),
  group_levels = NULL,
  ...
)
}
\arguments{
\item{df_long}{一个长格式的数据框，包含表型评分。应包含以下列："ID"、"group"、"GeneSet"、"Score"。}

\item{gene_set}{要绘制的基因集的名称。}

\item{x}{x轴变量的名称（默认是"group"）。}

\item{y}{y轴变量的名称（默认是"Score"）。}

\item{fill}{填充变量的名称（默认是"group"）。}

\item{palette}{用于绘图的颜色向量。}

\item{group_levels}{一个字符串向量，指定 group 的排序。}

\item{...}{传递给 \code{\link[ggpubr]{ggboxplot}} 的其他参数。}
}
\value{
一个代表箱线图的 ggplot 对象。
}
\description{
为单个基因集创建表型评分的箱线图。
}
\details{
该函数使用 \code{ggpubr::ggboxplot} 为指定的基因集创建表型评分的箱线图。可以通过传递额外的参数来自定义图形。
}
\examples{
# 示例用法：
\dontrun{
# 假设 df_long 是长格式的数据框
p <- plot_signature(df_long, gene_set = "GeneSet1", group_levels = c("A", "B", "C"))
print(p)
}

}
